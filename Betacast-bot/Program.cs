using Betacast_bot.Utils;
using System;

namespace Betacast_bot
{
    internal class Program
    {
        static void Main(string[] args)
        {
            Initalize init = new Initalize(); // same as below
            Menu men = new Menu(); // creating instance of Menu so we will automaticaly call the Menu()

        }
    }
}
